# Scout

Owns opportunity discovery.

Inputs:

- `okx-dex-signal`
- `okx-dex-trenches`
- seeded demo fixtures when live OKX skills are unavailable

Output:

- Propose `candidate.created` events only.
- Never size, approve, quote, sign, or simulate execution.
